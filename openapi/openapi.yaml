# Generated with protoc-gen-openapi
# https://github.com/google/gnostic/tree/master/cmd/protoc-gen-openapi

openapi: 3.0.3
info:
    title: LibraryService API
    description: A simple service for managing books.
    version: 0.0.1
paths:
    /api/v1/books:
        get:
            tags:
                - LibraryService
            description: Lists all books
            operationId: LibraryService_ListBooks
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/BooksList'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Status'
        post:
            tags:
                - LibraryService
            description: Adds a new book to the library
            operationId: LibraryService_AddBook
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/Book'
                required: true
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Book'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Status'
    /api/v1/books/{bookId}:
        get:
            tags:
                - LibraryService
            description: Gets a book by ID
            operationId: LibraryService_GetBook
            parameters:
                - name: bookId
                  in: path
                  required: true
                  schema:
                    type: string
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Book'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Status'
        put:
            tags:
                - LibraryService
            description: Updates an existing book
            operationId: LibraryService_UpdateBook
            parameters:
                - name: bookId
                  in: path
                  required: true
                  schema:
                    type: string
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/Book'
                required: true
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Book'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Status'
        delete:
            tags:
                - LibraryService
            description: Deletes a book by ID
            operationId: LibraryService_DeleteBook
            parameters:
                - name: bookId
                  in: path
                  required: true
                  schema:
                    type: string
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/DeleteBookRequest'
                required: true
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/DeleteBookResponse'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Status'
components:
    schemas:
        Author:
            type: object
            properties:
                givenName:
                    type: string
                familyName:
                    type: string
            description: Author message
        Book:
            type: object
            properties:
                bookId:
                    type: string
                isbn:
                    type: string
                    description: ISBN validation - simplified pattern for 10 or 13 digit ISBNs
                title:
                    type: string
                    description: Title validation - required, non-empty, max 200 characters
                authors:
                    type: array
                    items:
                        $ref: '#/components/schemas/Author'
                    description: Authors - at least one author required
                createdAt:
                    type: string
                    description: Timestamps for audit trail
                    format: date-time
                updatedAt:
                    type: string
                    format: date-time
            description: Book message
        BooksList:
            type: object
            properties:
                books:
                    type: array
                    items:
                        $ref: '#/components/schemas/Book'
        DeleteBookRequest:
            type: object
            properties:
                bookId:
                    type: string
        DeleteBookResponse:
            type: object
            properties:
                success:
                    type: boolean
        GoogleProtobufAny:
            type: object
            properties:
                '@type':
                    type: string
                    description: The type of the serialized message.
            additionalProperties: true
            description: Contains an arbitrary serialized message along with a @type that describes the type of the serialized message.
        Status:
            type: object
            properties:
                code:
                    type: integer
                    description: The status code, which should be an enum value of [google.rpc.Code][google.rpc.Code].
                    format: int32
                message:
                    type: string
                    description: A developer-facing error message, which should be in English. Any user-facing error message should be localized and sent in the [google.rpc.Status.details][google.rpc.Status.details] field, or localized by the client.
                details:
                    type: array
                    items:
                        $ref: '#/components/schemas/GoogleProtobufAny'
                    description: A list of messages that carry the error details.  There is a common set of message types for APIs to use.
            description: 'The `Status` type defines a logical error model that is suitable for different programming environments, including REST APIs and RPC APIs. It is used by [gRPC](https://github.com/grpc). Each `Status` message contains three pieces of data: error code, error message, and error details. You can find out more about this error model and how to work with it in the [API Design Guide](https://cloud.google.com/apis/design/errors).'
tags:
    - name: LibraryService
